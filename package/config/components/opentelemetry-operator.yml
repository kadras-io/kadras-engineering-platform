#@ load("@ytt:data", "data")
#@ load("@ytt:struct", "struct")
#@ load("@ytt:yaml", "yaml")
#@ load("/helpers.star", "is_package_additional")

#@ if is_package_additional("opentelemetry-operator"):

#@ def compute_package_values():
#@   values = struct.decode(data.values.opentelemetry_operator)
#@   return struct.encode(values)
#@ end

---
apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: opentelemetry-operator
  namespace: #@ data.values.platform.namespace
  annotations:
    kapp.k14s.io/change-group: opentelemetry-operator
    kapp.k14s.io/change-rule.cert-manager: upsert after upserting cert-manager
    kapp.k14s.io/change-rule.serviceaccount: delete before deleting serviceaccount
spec:
  serviceAccountName: kadras-install-sa
  packageRef:
    refName: opentelemetry-operator.packages.kadras.io
    versionSelection:
      constraints: 0.147.0
  values:
    - secretRef:
        name: opentelemetry-operator-values
---
apiVersion: v1
kind: Secret
metadata:
  name: opentelemetry-operator-values
  namespace: #@ data.values.platform.namespace
stringData:
  values.yaml: #@ yaml.encode(compute_package_values())

#@ end
